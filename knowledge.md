# knowledge.md

## Cross-Thread Patterns
- P0 | Trigger: 用户连续纠偏 / 局部修改 / 想继续加层 | Rule: 先回到核心契约，不先加层；只接受缩小契约偏差的改动 | Action: 复述目标、输入、输出、边界；若改动只会让方案更完整、不会让主链路更直接，回滚到最小主链路 | Failure: wrapper、状态、常驻、side effect 增加 | Recovery: 停优化，回到主链路
- P0 | Trigger: 需要网络检索 / web lookup / source gathering，且适合拆成独立 side task | Rule: 优先把检索子任务下发给 `spawn_agent`，模型默认用 `gpt-5.1-codex-mini`；主线程只保留约束、判断与整合 | Action: 先检查 skill 和 `knowledge.md` 是否已有现成 workflow，再让检索子代理回收来源、时间点和原文链接，最后由主线程做结论 | Failure: 主线程直接下场做重检索、或用更重模型跑轻量检索 | Recovery: 收回为 `gpt-5.1-codex-mini` 检索子任务，再由主线程复核整合
- P0 | Trigger: 需要多方案、独立审查、或防上下文污染 | Rule: 先抽共享约束，主线只处理共享部分；subagent 只做互不重叠的增量输出或独立复核 | Action: 单文件改写、单结论判断优先主线程；只有子任务能独立产出时才开 `subagents` | Failure: 为并行而并行，重复解释同一前提 | Recovery: 收回到主线程或只开必要子任务
- P0 | Trigger: 同时在做“原始需求 / 当前改动 / 修改建议”比对，或用户要求 n+1 / 独立回答 / 独立审查 | Rule: 先做三方对照，再决定改不改、怎么改；knowledge.md 只负责记忆规则，不替代当前回合执行 | Action: 当前回合需要的 gate 先执行，n+1 的额外 1 个子任务负责独立回答 + 审查；三者不对齐时先重写建议 | Failure: 只写规则不执行，或直接进入实现 | Recovery: 重新对齐三方输入
- P0 | Trigger: 用户明确要求 n+1 / 独立审查，即使任务看起来只是单文件维护 | Rule: 显式 n+1 优先于“单文件改写优先主线程”的低风险启发式；先开独立复核，再进入修改 | Action: 先让一个子任务独立判断，再由主线程落盘 | Failure: 把显式 n+1 降级成普通维护任务 | Recovery: 补做独立复核后再改
- P0 | Trigger: 调度 `spawn_agent` | Rule: 只能传 `message` 或 `items`，不能同时传 | Action: 先选一种载荷再发 | Failure: 参数校验失败 | Recovery: 重发为单一载荷
- P0 | Trigger: 用户明确要求用多个 `subagents` 避免上下文 / 思考链污染，或需要先分生态评估再做类比 | Rule: 先按角度拆成互不共享前提的独立轮次；第一轮只收各自领域判断，第二轮再拿主线程压缩后的背景分别做类比 / 反证 / 结构分类；已完成的子代理立即 `close_agent`，不要让旧上下文持续污染后续判断 | Action: 先抽共享背景并最小化，按 `领域评估 -> 关闭 -> 类比/反证 -> 再关闭` 推进；每个子代理只负责单一视角，主线程最后整合 | Failure: 同时把过多共享结论灌给所有子代理、让不同轮次复用未关闭旧代理、或把“高集中”直接类比成“垄断” | Recovery: 关掉旧代理，重开隔离子任务，只传最小背景和单一问题
- P0 | Trigger: 需要让知识条目稳定被 AI 识别 | Rule: 重要优先级标签要放在行首，`P0` 不要埋到行尾或括号里；格式优先于装饰 | Action: 先写 `P0 / P1 / P2`，再写内容 | Failure: 优先级信号变弱，后续扫描或模型读取时被漏掉 | Recovery: 把优先级提回行首并统一格式
- P0 | Trigger: 写入或修改 knowledge.md | Rule: 以后所有 knowledge.md 增删改都先检查相近 / 关联内容，按当前思路先收束，再决定增删改；`n+1` 独立复核由现有 P0 gate 负责，不并入这条 | Action: 先比对再改 | Failure: 重复入库、改错层级、漏掉关联规则 | Recovery: 回到相近内容比对后再动手
- P0 | Trigger: 需要恢复 / 回滚 / 重建 knowledge.md | Rule: 恢复前必须先过 checklist，不能把“当前随手找到的版本”当 source of truth | Action: checklist = `1 先查工作区现有文件 2 列所有候选 session/backup/副本 3 按时间戳排序 4 标清版本类型（分级版 / 专题摘要版 / 增量版） 5 锁定本次会话开始前的最近正确版本 6 先备份当前文件再恢复；若要混合回填，只能在正确基线恢复后追加` | Failure: 把 Documents 副本、旧快照、专题摘要或本次错误产物误当主库，导致连续错回滚 | Recovery: 回到 checklist，从候选时间线重新锁定正确基线后再操作
- P0 | Trigger: 整理 Google Photos 公开分享链接 | Rule: 只处理当前链接和当前下载集，不回调旧 Codex session；先读 HTML 源码，再读 `AF_initDataCallback`，最后只看本次新下载到隔离临时目录的图片 | Action: 先定位公开元数据，再拆 SSR 数据，再做本次图片的视觉 / OCR 归纳 | Failure: 旧 `/tmp` 内容混入、把旧 session 当当前证据、或先看渲染结果后回头补源代码 | Recovery: 清空并重建当前临时目录，只按当前链接重跑一遍
- P0 | Trigger: 自我概念 / 身份 / 边界 / 关系 / 注意力 / 习惯材料要入库 | Rule: 先拆 `核心判断 / 方法动作 / 隐喻修辞`，把愿望句改成身份句或动作句，证据不足标 `假设`，最后只留一条最小 stage card | Action: 先写 `kernel / method / minimum action`，再合并同义项 | Failure: 把修辞当规则、重复建卡、保留说明式小节 | Recovery: 回到最小卡片和现有知识条目
- P0 | Trigger: 任何新闻 / 公告 / 报道 / 帖子要快速评估 | Rule: 先做 `1 定位来源层级 -> 2 拆事实/推断/立场/缺口 -> 3 再下 grounded/inference 结论`；`knowledge.md` 只存规则，不当事实库 | Action: 先找最上游来源和时间点，再写三栏，最后再给结论强度 | Failure: 先被标题或旧知识带跑，或把知识库里的规则当新事实 | Recovery: 回到原文、证据链和时间线
- P1 | Trigger: 跨厂商比较模型价格 / 上下文 / 评价 | Rule: 先统一到 `USD per 1M tokens`，再单列 `subscription / quota` 型产品；如果官方文档对上下文口径不一致，记安全范围，不硬写单点数 | Action: 先标注 `token-priced` / `quota-based` / `N/A`，再换算到同一标准 | Failure: 把配额制当按量计费、把不同上下文窗口混成一个数 | Recovery: 回到官方计费页和模型页重新对齐口径
- P1 | Trigger: AI Coding / Spec Coding 直接按 PRD 落码 | Rule: 把 AI 当实现器，不当业务边界发现器；规格冻结前必须有人先 review `PRD / 现网逻辑 / 跨端差异 / 边界场景`，再让 AI 生成代码 | Action: 先补 scope 和回归矩阵，再验代码；验收时单列“这次覆盖了什么”和“这次没写进 PRD 但可能受影响的地方” | Failure: 把“代码能跑”当“需求已覆盖” | Recovery: 回到 PRD、现网逻辑和边界场景逐项补齐
- P1 | Trigger: 未公开招股书阶段的 mega-IPO 帖子带具体 `float / retail allocation / fill rate / lock-up` 数字 | Rule: 先分 `已确认公开文件`、`主流媒体匿名消息`、`基于假设的算术推演`；只要还没有公开 prospectus / S-1，精确条款默认不稳定 | Action: 没有公开招股书时，不把 `5% float`、`30% 散户配售`、`免锁定`、个人中签率当已定事实 | Failure: 把传闻参数和二次演算写成确定结构 | Recovery: 回到 SEC/交易所公开文件和最上游报道层级
- P1 | Trigger: 比较 agent 产品 | Rule: 拆成 `control surface`、`execution surface`、`trust model` 三条轴，不要只按“是不是 agent”做一维比较 | Action: 先分控制面、执行面、信任模型，再写相似点和差异 | Failure: 用单一模型名或单一产品名硬对比整个产品栈 | Recovery: 回到三轴框架重排
- P1 | Trigger: 比较 2026 年后的 OpenAI 新产品 | Rule: 不要只盯模型名，默认拆 `ChatGPT agent`、`Codex app`、`GPT-5.3-Codex` 三层再判断 | Action: 先写绝对发布日期，再拆 `model / app / agent` | Failure: 用“latest”或单一名字混掉层级 | Recovery: 回到发布日期和产品栈分层
- P1 | Trigger: 询问 OpenAI / Codex CLI 某个新模型“能不能用 / 能不能访问” | Rule: 先分 `CLI model-awareness`、`auth/session`、`backend execution` 三层验证；先看本地 `debug models` / `--help` 是否识别模型，再看 `codex login status`，最后用最小 `codex exec -m <model>` 做端到端试跑；结论必须写成“能否访问 + 卡在哪一层”，不要只回 yes/no | Action: 先查目录与登录，再做最小可执行探测 | Failure: 把“能传参数”误当“能成功访问”，或把后端 5xx 误判成登录问题 | Recovery: 回到三层检查并标明阻塞层
- P1 | Trigger: 用户给的是二手报道 | Rule: 先映射回一手产品帖、公司公告或原始研究，再做能力比较或战略判断 | Action: 找最上游来源，再做分析 | Failure: 直接拿二手报道的 framing 当事实层 | Recovery: 回到 primary source
- P1 | Trigger: `bank note + stock move + news catalyst` 请求 | Rule: 先拆 `季节性/基础业务` 与 `增量叙事`，再按 `PC / data center / cloud / edge / security / capital structure` 重分每个催化 | Action: 先识别 baseline，再重分桶每个新催化 | Failure: 把旧周期和新叙事混为一谈 | Recovery: 回到 revenue lane 和 timing 逐项重算
- P2 | Trigger: 在 macOS 上跑常驻 worker | Rule: 优先 `launchd`；secret / 运行参数放独立 env 文件；真实 worker 用 wrapper；编译检查把 `PYTHONPYCACHEPREFIX` 指到 `/tmp`；Python 3.9.6 下避免 3.10+ 语法 | Action: 让 worker / bridge 通过 LaunchAgent 启动，保持环境隔离 | Failure: shell profile 污染、权限问题、缓存目录问题 | Recovery: 退回独立 env 和 launchd wrapper

## Meta / Process
- [P2] [2026-04-07] Telegram / WhatsApp 本地接口规划 | Intent: 不搭服务器，把 Telegram / WhatsApp 只当输入输出接口，处理留在本地 Codex | Main conclusion: 先做 Telegram 路线更稳，因为本地长轮询可以直接跑在常驻 worker 里；MVP 里建议 `telegram_bridge + codex_worker` 两进程，`poller / dispatcher / sender` 先合并在 bridge 内 | Reusable takeaway: 这类任务先做共享基线和重复部分分析，再按“架构 / 实现”或“平台 A / 平台 B”拆 subagent。共享部分留给主线，子任务只做非重叠输出；另外先写独立接口规范文件，再进实现。实现顺序优先是 `schema -> state machine -> store -> bridge/worker skeleton -> smoke test`。编译检查时把 `PYTHONPYCACHEPREFIX` 指到 `/tmp`，可以避开系统缓存目录权限问题。worker 在启动本地命令前要先显式检查 `workspace_root`。如果 reply queue 失败，completed job 直接转 `failed` | Open item: WhatsApp inbound message path 是否能完全在无公网常驻服务的前提下成立，需要继续验证
- [P2] [2026-04-23] 向上汇报模板按场景分流 | Intent: 从公开相册里的管理沟通卡片提炼可复用框架 | Main conclusion: 这组卡片不是一条线性 SOP，而是按沟通场景选模板：对齐领导意图时先抓 `leader intent` 和痛点，再用 `SMART` 收口；要资源 / 提需求时带方案，用 `RIDE` 说明 `risk / interest / difference / effect`；正式汇报结果时用 `PREP` 先给结论再补理由；复盘时用 `GRAI` 做 `Goal / Result / Analysis / Insight`；分享流程时用 `5W2H` 把场景拆全 | Reusable takeaway: 不要把“汇报、提需求、复盘、分享”混成同一种话术，先定目标读者和输出物，再选最短模板；越往上级沟通，越应该先讲结论和结果，再补细节 | Notes: 公开页可确认 `Link sharing is on` 和 `Hao Chen` 为所有者；第一个分享链接只稳定暴露到泛化相册元数据，未拿到可读的公开图片内容，因此未并入主结论

## Research / Verification
- [P2] [2026-04-07] 盘后科技股、`QQQ`、`XAU/USD` 与五角大楼简报取消 | Intent: 解释纽约时间 `4pm` 收盘后科技股 / 大盘 / `QQQ` 走强的原因，并查看 `XAU/USD` 在同一时间窗的变动；后续追问五角大楼为什么取消简报会 | Main conclusion: 盘后拉升主要对应 Trump 对伊朗 / 霍尔木兹海峡的停火或缓和信号，市场把“军事升级延后”定价为风险偏好回升；`XAU/USD` 同时上行，五角大楼取消简报会的官方原因未公开，因此相关解释只能写成推断 | Reusable takeaway: 处理类似盘后新闻，先把 `4pm ET` 当作固定分界，再分别记录盘中、盘后、隔夜三段行情；若只有新闻标题没有官方说明，结论要分成“已确认事实”和“推断”两层 | Uncertainty: 五角大楼没有公开解释取消简报会的具体原因；Trump 的说法也有反复，不能把“停战两周”当成稳定承诺
- [P2] [2026-04-16] GPT-Rosalind 与 Codex 扩展比较框架 | Intent: 整理同日 OpenAI 产品更新里 `GPT-Rosalind` 与更广义 Codex 扩展的关系 | Main conclusion: 领域模型发布和 agent / app 平台发布要分开看；涉及“最新”时要直接写绝对日期，不要只用相对说法 | Reusable takeaway: 分析 OpenAI 产品栈时，先拆 `model`、`app`、`agent` 三层，再谈战略位置或能力对比 | Uncertainty: 当时的本地复核不算完整；以后复用时，底层事实仍应回到一手来源再核实
- [P2] [2026-04-16] Perplexity Personal Computer 与 OpenAI agent / Codex 的对应关系 | Intent: 比较 Perplexity Mac 上的 `Personal Computer` 与 OpenAI 最接近的能力面 | Main conclusion: 最接近的 OpenAI 对应物不是单一产品。泛化的电脑 / 网页任务执行更接近 `ChatGPT agent`，而委托型技术工作更接近 `Codex app` | Reusable takeaway: Perplexity `Personal Computer` 更像 system-adjacent personal agent；OpenAI 则把市场拆成更广的 agent surface 与更技术化的 delegated surface | Fact-check notes: `Everything is Computer` 是理解 Perplexity 新 framing 的关键一手材料；Google `AI Mode in Chrome` 更像搜索 / 浏览器 UX 层，不能并入同一个产品桶
- [P1] [2026-04-16] OpenAI 与 Google / DeepMind 在生命科学 AI 的竞争结构 | Intent: 把 `GPT-Rosalind` 对 Google / DeepMind 的比较压成可复用的竞争结构判断 | Main conclusion: 这不是单一公司已形成垄断的市场。Google / DeepMind 通过 `AlphaFold`、`TxGemma` 等已有很强位置；OpenAI 更像后来进入者，重点在 `trusted access` 和垂直 workflow embedding | Reusable takeaway: 遇到“新模型挑战 incumbent”叙事时，先拆 `market structure`、`business model`、`analogy`；若已有强对手和开放生态，默认判断应是 `高壁垒竞争 + 局部锁定风险`，不是“已建立垄断” | Fact-check notes: OpenAI 更现实的风险是少数高价值机构上的局部 lock-in，而不是全市场控制；更稳的类比是 `Google = open ecosystem + base capability layer`、`OpenAI = vertical workflow embedding`

## Tech / Finance / Product
- [P1] [2026-04-09] iOS 原生键盘 vs Gboard 的输入法降噪 | Trigger: 处理输入法变笨和候选污染 | Rule: 问题通常在建议、词库和误选污染，不在核心打字引擎 | Action: 先控输入源，再管 learned state；iOS 偏向少键盘 / 少语言 / 重置词典，Gboard 偏向限制建议 / 手动加词 / 清空已学词 | Failure: 继续自动接受错误候选 | Recovery: 停自动接受，补正确词，必要时重置词典
- [P2] [2026-04-13] Outlook Lite 关停时间线 | Intent: 核实“微软将于 5 月 26 日正式关闭 Outlook Lite”这条消息，并补一段可直接复用的产品历史 | Main conclusion: Outlook Lite 于 2022-08-01 公布，最初面向低配 Android 和弱网场景；微软公开支持页目前仍写“自 2025-10-06 开始 retired，现有用户可在有限时间内继续使用”，但媒体报道称微软已确认现有用户会在 2026-05-26 失去功能，因此最稳妥写法应拆成“2025-10-06 下架 / 2026-05-26 停服（据微软向媒体确认）” | Reusable takeaway: 遇到产品退役新闻，若官方静态支持页和媒体报道时间点不一致，先保留双日期并标明证据层级，不把旧支持页上的“retired starting”直接等同于最终停服日 | Open item: 公开可见的微软支持页是否会补上 2026-05-26 的明确日期，仍需后续复核
- [P2] [2026-04-13] Major Hardware Superdome 15 风扇侧板 | Intent: 记录 Major Hardware 把 15 把 Noctua NF-A12x25 风扇打包进 3D 打印的 Superdome 球形侧板并作为正压进风试验 | Main conclusion: Noctua 官方赞助风扇和配色耗材后，Superdome 能在联力 O11 机箱上稳定工作，跑《战地6》时 CPU 温度从 86℃ 降到 67℃，噪音不明显但线材布置必须避免扫叶 | Reusable takeaway: 极端散热改造可以先复用既有“Fanhattan”框架，确保打印尺寸适配再批量铺设 120mm 风扇，用正压风道挤压大量进风；高成本时先争取赞助或确认替代方案，线材整理是长期最容易被忽略的隐患 | Open item: 不同机箱布局或风道组合下是否都能保持大幅温度降低，以及长期正压是否会带来比对流更高的灰尘成本，仍需实际复刻 / 检验
- [P1] [2026-04-14] AI Coding 对产品文档质量的反向约束 | Intent: 把一组关于 pure Spec Coding 技术评审和评论区讨论压成可复用结论 | Main conclusion: AI Coding 能按规格高效落码，但不会稳定补出 PRD 漏掉的线上逻辑、跨端差异和边界场景；单次没出严重 bug 往往是共用代码侥幸覆盖，不是 AI 主动发现缺口 | Reusable takeaway: AI 更像高执行实现器，不是业务范围发现器；规格阶段先做 `PRD 评审 + 现网逻辑 review + 边界补齐`，否则人会把注意力缩到“代码对不对”，漏掉“需求有没有漏” | Open item: 以后若要验证“AI 能否补全全量功能点”，要分开评估 `已知的未知` 和 `未知的未知`
- [P1] [2026-04-16] Bernstein 图片、股价与 Intel / AMD 新合作的催化重分桶 | Intent: 把券商预览图、近期股价走势和 Intel / AMD 新合作消息放到同一分析框架里 | Main conclusion: 图里给的是偏弱的 `Q1 PC seasonal` 基线，但股价波动更像由 `AI infrastructure / cloud / edge / security / foundry` 叙事驱动，而不只是 PC 需求。新合作只在局部对冲弱 PC 季节性，因为大多数催化不在 client-PC 桶里 | Reusable takeaway: 当图表显示弱季节性 PC 设定时，不要默认半导体股票还在交易 PC 主线。先把每个新催化按收入路径和兑现时点重分桶，再判断市场在重定价 `near-term PC shipments` 还是别的中期叙事 | Fact-check notes: 合作消息先用公司一手公告，股价再用行情页确认；若股价波动明显大于基础业务变化，优先排查 `capital structure`、`AI platform`、`cloud customer` 等催化
- [P2] [2026-04-23] 大宗商品涨价顺序与三轮周期图 | Intent: 从图片整理大宗商品上涨顺序、历史周期验证和特殊情况 | Main conclusion: 图里的主链条是 `黄金 -> 白银 -> 铜 -> 铝 -> 钢铁 -> 原油 -> 化工品 -> 农产品`，对应的分类逻辑是 `金融属性 -> 工业属性 -> 能源消费属性 -> 民生刚需属性`。它还用 2008-2011、2016-2018、2020-2022 三轮周期做例子，表达的是“贵金属先行，随后工业金属与能源，最后扩散到农产品”的常见顺序 | Reusable takeaway: 看 commodity basket 时先判断驱动是流动性/避险、工业复苏、能源成本传导还是供给冲击，再决定谁是先行指标；特殊情况下顺序会被供给中断、天气、政策干预或板块联动打乱，不能把图里的顺序当成放之四海而皆准的定律 | Notes: 这是一张社媒信息图，时间段和涨幅属于示例化归纳；复用时应回到价格序列和官方/原始数据核对
- [P2] [2026-04-16] 金属 3D 打印是怎么实现的 | Intent: 解释金属 3D 打印的实现机制 | Main conclusion: 本质是把三维模型切成很多薄层，再用激光、电子束、粘结剂或送丝 / 送粉能量源，按层把金属材料熔化、烧结或成形，最后得到实体零件 | Reusable takeaway: 主流路线可分为粉床熔融、定向能量沉积、粘结剂喷射和材料挤出 / 间接烧结；差异主要在能量输入方式、精度、速度、成本和后处理强度 | Notes: 金属打印通常不是“打印完直接可用”，常需要去支撑、热处理、机加工和表面处理
- [P2] [2026-04-16] 金属打印和塑料 FDM 的区别 | Intent: 对比金属 3D 打印与塑料 FDM | Main conclusion: 两者都按层制造，但金属打印依赖高能量熔化 / 烧结金属粉末或丝材，工艺复杂、成本高、性能强；塑料 FDM 主要是把热塑性材料加热软化后挤出堆叠，设备便宜、门槛低、性能较弱 | Reusable takeaway: 核心区别集中在材料态、成形能量、设备复杂度、尺寸精度、力学性能、后处理要求和应用场景 | Notes: FDM 更像“热熔胶枪式堆积”，金属打印更接近“微型焊接 / 烧结 / 冶金成形”
- [P2] [2026-04-23] 小红书短链 / 公开页提取 | Intent: 处理 `xhslink.com` 短链时，尽可能还原到原始笔记并提取可公开读取的信息 | Main conclusion: 先用短链拿到原始 `xiaohongshu.com/discovery/item/...` 地址，再从页面 `og:*` meta 和 SSR 内嵌状态里抓 `title`、`desc`、作者昵称、发布时间、点赞 / 收藏数、标签和图片 URL；正文通常能在 `desc` 里直接读到，完整评论区和登录后内容不一定可见 | Reusable takeaway: 遇到社媒短链，优先找“公开页 + 页面源代码”而不是只看浏览器渲染结果；把能稳定拿到的字段当作公开元数据，别把一次抓到的正文当成长久稳定接口 | Notes: 这类页面常把图片列表和笔记内容直接塞在 HTML / JSON 里，适合用 `curl` + `rg` 先定位 `og:title`、`og:description`、`desc`、`user`、`urlDefault`
- [P2] [2026-04-23] Google Photos 公开相册提取 | Intent: 处理 `photos.app.goo.gl` / `photos.google.com/share/...` 公开相册时，尽可能整理出相册标题、分享状态、所有者和图片清单 | Main conclusion: 公共分享页的 HTML 往往就有可用元数据：`og:title` 会给出相册标题或“几张新照片 + 日期”的摘要，`og:description` 常是 `Tap to view!`，`og:image` 给封面图；更完整的信息通常藏在 `AF_initDataCallback` 的 SSR 数据里，可以直接拿到分享密钥、所有者名称、头像、单张图片的 `lh3.googleusercontent.com` URL、尺寸和时间戳 | Reusable takeaway: 抓 Google Photos 公开页时先看 HTML 源码，再从 `AF_initDataCallback` 里拆相册结构，不要只依赖浏览器渲染；结论最好写成“标题 / 所有者 / 图片数 / 公开可见字段 / 仍缺失字段” | Notes: 这类分享页常能确认 `link sharing is on`、Owner 名称和缩略图数组，但图片正文、说明文字或更深层元数据未必全部公开
- [P2] [2026-04-23] Austin Belcak 求职高 ROI 卡片 | Intent: 从公开相册里的求职截图卡片提炼可复用的 job search 优先级框架 | Main conclusion: 这组卡片把高回报动作排成一条链：先 `personalize resume`，再做 `company research`，然后 `engage on LinkedIn`，接着 `build a portfolio`、`create content`、`prepare for interviews early`；其中 `company research` 又可细拆成 `earnings calls`、`executive interviews/podcasts`、`product reviews`、`news articles`、`customer reviews`、`competitive analysis`、`current employees` 7 个信息源 | Reusable takeaway: 找工作时不要把时间平均撒在海投、改简历和刷岗位上，优先把精力集中在“目标公司定制化 + 关系触达 + 作品证明 + 早做面试准备”这几类高杠杆动作上；公司研究越具体，后面的简历、作品集和面试回答越容易对齐 | Notes: 这组图来自 Austin Belcak 的黑底卡片截图；公开相册里可见 8 张图，但活动编号并不完全连续，说明原帖可能还有一张未被截入或是被合并展示
- [P2] [2026-04-23] 海外职场“健康攻击性”卡片 | Intent: 从小红书职场截图里提炼可复用的向上管理、可见度和资源表达框架 | Main conclusion: 真正有效的做法不是硬抢功，而是把工作进展、结果、风险和合作贡献说清楚：项目开始先管理老板预期；汇报时用简短进度 + 具体产出 + 数据 / 里程碑；遇到协作就点名他人并把自己的主导权写明；需要资源或反馈时把诉求拆成具体可执行的问题；社交用于建立记忆点，但不替代实绩 | Reusable takeaway: 在海外职场里，可见度来自“清晰叙述自己的工作进展、影响和下一步”，不是空泛自夸；优先用事实、阶段、数据、风险和协作成果说话，避免只说“我很努力” | Notes: 这是一组社媒截图，属于经验性建议，不当作普适规律；已按 7 张图核对为同一条完整叙事，未见明显冲突页
- [P0] [2026-04-23] 网页 / 帖子整理入库工作流 | Intent: 处理用户给出的网页、帖子或公开链接时，先抽取图片、正文和评论区，再清洗、校验并筛出可复用信息 | Main conclusion: 默认顺序是 `打开网页 -> 抽取图片 / 正文 / 评论 -> 清洗数据 -> 联网 fact check -> 只保留合理有效的信息 -> 入库 P0`；事实不稳、来源弱、或只能靠单条帖子自证的内容要降权或丢弃，不把噪音写进知识库 | Reusable takeaway: 这类任务的核心不是“把所有内容抄下来”，而是先筛信息再入库，优先保留可公开验证、可跨帖复用、且足够稳定的结论；如果用户要求“正式开始”，可用触发词 `整理信息 验证合理性 修改后 入库P0`，或简写 `网页整理入库` | Notes: 评论区只在能补充事实链或反证时纳入；正文、图片和评论之间若互相冲突，以可验证的一手来源和时间线为准
- [P1] [2026-04-23] 网页 / 帖子入库里的 P2 含义纠偏 | Intent: 明确前一条工作流里的 `P2` 不是任务优先级，而是整理后的信息层级 | Main conclusion: 在这类任务里，`P0` 表示工作流要求或任务级别，`P2` 只指清洗、筛选后保留下来的信息内容，不要把原始材料、噪音或未经 fact check 的内容误写成 P2 | Reusable takeaway: 以后标记这类任务时，优先级写 `P0/P1`，内容层级另行说明为“整理后的信息”；避免把优先级和信息分层混用 | Notes: 若上下文不清，先问清是“任务优先级”还是“信息层级”

## Open Items
- [P2] 以后复用 OpenAI 产品比较前，再核实是否已有更新的一手帖子替代 `2026-02-02` 的 Codex app 定位。
- [P2] 如果下次要矩阵，默认列为 `scope`、`execution environment`、`permissions`、`background work`、`best-fit use case`。
